import RestaurantList from "./RestaurantList";

const Body = () => {
  return (
    <main className="mainContainer">
      <section className="centerAlign">
        <RestaurantList />
      </section>
    </main>
  );
};

export default Body;
